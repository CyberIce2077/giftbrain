.box-container
  .box
    %div{ data: { controller: "calendar", turbo_stream: true }, id: "calendar" }
    .hint.center-element Recurring events are automatically moved to next year after their date passes
= turbo_frame_tag("recipients")
