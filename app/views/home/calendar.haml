.box-panel.float-left
  .box-container
    %div{ data: { controller: "calendar", turbo_stream: true }, id: "calendar" }
    .hint.center-elements Recurring events are automatically moved to next year after their date passes
.box-panel.float-left
  = turbo_frame_tag("recipients")
