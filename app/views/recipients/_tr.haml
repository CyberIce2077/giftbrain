= turbo_frame_tag(recipient) do
  .box-container
    .box
      %h1= recipient.name
      = recipient.status
      %p= recipient.description
      .box
        - if policy(recipient).show?
          = link_to('Show', [recipient], class: 'is-button', data: { turbo: false })
