default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: giftbrain_development

test:
  <<: *default
  database: giftbrain_test

production:
  primary:
    <<: *default
    host: db
    username: giftbrain
    password: <%= ENV["GIFTBRAIN_DATABASE_PASSWORD"] %>
    database: giftbrain_production
